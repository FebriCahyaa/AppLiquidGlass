# Flutter implementation

This is a complete Flutter target for the Liquid Glass experience.

## Run

```bash
flutter pub get
flutter analyze
flutter test
flutter run
```

The Flutter target is intentionally separate from the Android Kotlin application. Share visual tokens and assets, but do not embed Flutter inside the native Android Activity.
